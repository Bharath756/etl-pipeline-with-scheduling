# etl-pipeline-with-scheduling
Production-grade ETL pipeline that ingests raw data, applies transformations, loads it into a relational database, and runs on a scheduled workflow with logging, retries, and monitoring.
